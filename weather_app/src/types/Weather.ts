
// Type to pass to WeatherService for fetching alerts from different endpoints
export enum AlertFilterType {
    Area = 'area',
    Zone = 'zone',
    Region = 'region'
}

// TODO add types for responses

// TODO add enums for zoneIds, areas, regions?? need to find all possible values.. maybe there's a library for the types?